apiVersion: v1
kind: Service
metadata:
  name: {{ include "basyxstarterapplication.fullname" . }}
  labels:
    {{- include "basyxstarterapplication.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "basyxstarterapplication.selectorLabels" . | nindent 4 }}
