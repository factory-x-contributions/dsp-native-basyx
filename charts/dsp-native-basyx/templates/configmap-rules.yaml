apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dsp-native-basyx.fullname" . }}-rules
  labels:
    {{- include "dsp-native-basyx.labels" . | nindent 4 }}
data:
  rules.json: |-
{{ .Files.Get "files/rules.json" | indent 4 }}
